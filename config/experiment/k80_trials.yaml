name: k80_trials
split_id: three_way_plus_holdout_v1
seed: 42
# Manifest root: runs/<runs_subdir>/ (default pipeline when null).
runs_subdir: null

models:
  - gemma-3-4b

directions:
  - minimize

feature_counts:
  - 80

trial_grid:
  "80": [500, 1500, 3000]

stages:
  extract_activations: false
  feature_selection: false
  optimization: true
  ipi_baseline: true
  ipi_intervened: true
  poeta: false
